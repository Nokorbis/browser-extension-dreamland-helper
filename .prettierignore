# Generated by WXT — regenerated on every `wxt prepare`.
.wxt/
.output/
stats.html

# Build/dependency artefacts.
node_modules/
pnpm-lock.yaml

# Generated data asset: committed, but produced by `pnpm gen:emoji`, not hand-edited.
# See docs/adr/0022-lazy-loaded-data-assets.md.
public/emoji/emoji.json

# Captured live-forum HTML kept for checking selectors by hand. Not ours to format.
real_snippets/

# Binary/vector assets.
*.svg
*.png

# Prose. Hand-wrapped throughout, and `docs/adr/` is immutable once accepted — a record
# that gets reflowed by a tool is no longer the record that was reviewed. Prettier's only
# effect here was rewriting `*emphasis*` to `_emphasis_` across every doc, which is churn
# with no reader benefit.
*.md

# The user-facing string catalogue. Deliberately hand-maintained: @wxt-dev/i18n parses
# `{name}` in a value as a *named substitution*, so its content is load-bearing in ways a
# formatter can't see, and reflowing it would buy nothing but risk. See CLAUDE.md.
src/locales/*.yml
